{% extends 'back.html.twig' %}

{% block title %}Detail Genre{% endblock %}

{% block content %}
    <table class="table">
        <tbody>
            <tr>
                <th>Category_id</th>
                <td>{{ category.category_id }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ category.label }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('category_index') }}">back to list</a>

    <a href="{{ path('category_edit', {'category_id': category.category_id}) }}">edit</a>

    {{ include('category/_delete_form.html.twig') }}
{% endblock %}
